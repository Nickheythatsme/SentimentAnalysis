/*
 * 5/23/2018
 *
 * A thread safe queue with packages. Each package stores the arguments for
 * one specific run of the function, and the return value from that run. After
 * packages are removed from the queue they are added to the finished vector.
 */

#include <mutex>
#include <atomic>
#include "job.h"

template<class R, class A>
class queue
{
    public:
    protected:
    private:
};

#ifndef QUEUE_QUEUE_
#define QUEUE_QUEUE_

#endif // QUEUE_QUEUE_


